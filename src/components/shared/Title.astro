---
interface Props {
  title: string;
  className?: string;
}
const { title, className } = Astro.props;
---

<span
  class={`text-3xl md:text-5xl font-bold text-center mb-10 text-textPrimary ${className}`}
>
  {title}
</span>
